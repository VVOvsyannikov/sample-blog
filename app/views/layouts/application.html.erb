<!DOCTYPE html>
<html>

<head>
  <title>Blog</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <div class="container">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand mb-0 h1" href="/">My Blog</a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" href="/articles">Articles</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/articles/new">New article</a>
            </li>
          </ul>

          <% if user_signed_in? %>

          <div class="navbar-text me-2">Hello, <%= current_user.username %> </div>

          <%= button_to 'Sign Out', destroy_user_session_path, class: "btn btn-sm btn-outline-secondary", method: :delete%>

          <% else %>

          <%= button_to 'Sing In', new_user_session_path, class: "btn btn-sm btn-outline-secondary", method: :get %>

          <% end %>

        </div>

      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="row">

      <%= yield %>

    </div>
  </div>
</body>

</html>